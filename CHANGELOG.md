# Changelog

All notable changes to the `stride-ideation` plugin are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Initial plugin scaffold: layout mirrors `security-review` (commands/, skills/stride-ideation/, agents/, .claude-plugin/plugin.json).
- Placeholder files for the three slash commands (`/stride-ideation:ideate`, `/stride-ideation:decompose`, `/stride-ideation:ship`), the ideation skill, and the requirements decomposer and reviewer subagents. Real content lands in downstream tasks per the design spec.
